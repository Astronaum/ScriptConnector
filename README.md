Changing the parameters in scripts to match the schema defined in schema.ps1 so the demo can work.

Available features :
  - Running Powershell, python, perl and bin\bash scripts
  - Script's hash verification
  - Outbound Mapping available
